# 微信读书公众号全量文章链接提取流程

更新时间：2026-08-05。该流程已实测跑通：单号一次可取数千条（例：国科大就业 1414 条，2024-09-25 ~ 2026-08-05）。

## 实测结果（2026-08-06，11/11 账号全部成功）

| 公众号 | 链接数 | 收录范围 |
| --- | ---: | --- |
| 成功就业 | 873 | 2024-10-24 ~ 2026-08-05 |
| 国科大就业 | 1414 | 2024-09-25 ~ 2026-08-05 |
| 北航就业 | 1083 | 2024-12-26 ~ 2026-08-04 |
| 成电就业 | 1147 | 2024-12-13 ~ 2026-08-04 |
| 北大就业 | 859 | 2025-04-19 ~ 2026-08-05 |
| 西安交大就业创业 | 1371 | 2025-04-15 ~ 2026-08-05 |
| 川大就业 | 1427 | 2025-01-16 ~ 2026-08-05 |
| 人大就业创业 | 828 | 2024-11-01 ~ 2026-08-05 |
| 浙大就业 | 1279 | 2025-04-15 ~ 2026-08-05 |
| 西电科大就业指导服务中心 | 1290 | 2025-03-27 ~ 2026-08-05 |
| 西工大就业 | 1492 | 2024-09-20 ~ 2026-08-05 |
| **合计** | **13063** | 全部为唯一 mp.weixin.qq.com 直链 |

## 适用与不适用

- 适用：批量获取公众号已发布的文章**链接**（标题 + 发布日期 + `mp.weixin.qq.com/s/...` 原文直链），不读正文。
- 不适用：拿不到微信读书未收录的内容（部分号收录滞后、缺漏；收录起点一般晚于号创建时间）。要绝对全量历史，仍需微信客户端“全部消息”滚动方案。
- 重要：微信读书对公众号的收录是**持续回填**的，并非一次到位。实测多个账号首次全量拉取时每号仅收录约
  200 个群发日，之后以“整天”为单位陆续回填（散布在全部历史中，无半天缺失——说明不是翻页漏抓，
  而是微信读书当时尚未收录）。因此增量运行时每页（约 50 个群发）总会“发现”几篇从未见过的旧文章，
  这是正常现象；深翻到空页即说明已覆盖微信读书**当前**已收录的全部内容。
- 风险：依赖微信读书网页接口，腾讯随时可能收紧；频率过高会触发验证码/风控（单日多次大批量拉取有风险）。个人低频使用问题不大。

## 原理

1. 公众号任意文章页 HTML 里有 `var biz = "<__biz>"`（base64）。
2. 微信读书里公众号的 `bookId = "MP_WXS_" + base64解码(__biz)`（解码结果是数字）。
3. 微信读书网页接口 `/web/mp/articles?bookId=...&offset=N` 返回文章列表，每篇带 `originalId`，拼成 `https://mp.weixin.qq.com/s/<originalId>`。
4. 该接口要求在“公众号阅读器页”（`/web/reader/<hash>`）的上下文调用，否则返回 -2041。阅读器 hash 从书架接口的 `deepLink` 的 `v` 参数得到。

## 前置条件

- Ubuntu 24.04、macOS 或 Windows 的 Chrome/Chromium + 微信读书账号。
- 本机已有文章链接候选：公众号的任一一篇公开文章链接

## 步骤

### 1. 准备账号清单

编辑 [`微信读书提取配置.json`](./微信读书提取配置.json)，每个公众号给 `name` 和一篇 `url`。

### 2. 启动独立 Chrome 并登录微信读书（只需一次）

推荐双击或在终端运行固定启动器：

```sh
./start_weread_chrome.sh
```

它会把专用 Profile 固定保存在：

```text
.browser-profile/weread
```

该目录包含登录 Cookie、设备标识和浏览记录，已由 `.gitignore` 排除；不要提交、分享或同步给其他人。
只要一直使用这个启动器并正常关闭 Chrome，登录状态会保存在同一个 Profile 中。微信读书仍可能让服务端
会话自然过期，届时需要重新扫码，但不会因系统清理临时目录而必然丢失。

若未自动找到浏览器，可设置 `WR_CHROME_BIN` 后再运行启动器，例如 `WR_CHROME_BIN=/usr/bin/google-chrome ./start_weread_chrome.sh`。旧的手动启动命令如下，仅供排错参考；不要再把长期 Profile 放在临时目录：

```sh
google-chrome \
  --user-data-dir="/固定且私有的目录/weread-chrome" \
  --remote-debugging-port=9223 \
  --no-first-run --no-default-browser-check
```

打开 https://weread.qq.com/ ，微信扫码登录。用独立配置目录是为了不影响日常 Chrome。

> 登录动作必须由本人完成（扫码或验证码）；脚本不会读取或导出 Cookie 内容。

### 3. 运行提取脚本

正式抓取前可先做一次低频会话自检（只同步书架，不读取文章）：

```sh
node weread_extract.mjs 9223 --check-session
```

输出 `ok: true` 且 `shelfAccounts` 为目标公众号数量时，再开始正式更新；若为 false，先在专用 Chrome
中完成登录或验证码，不要连续重试抓取命令。

在项目根目录直接运行：

```sh
node weread_extract.mjs 9223
```

运行需要放行两件事：

- 连接本机 Chrome 调试端口 `127.0.0.1:9223`；
- 访问 `mp.weixin.qq.com`（解析 __biz）和 `weread.qq.com`（拉列表）。

脚本会逐号：解析 bookId → 加入微信读书书架 → 打开阅读器页 → 翻页拉取 → 写 CSV/MD，最后生成汇总与运行报告。请求间隔已内置（约 1.2s/页、2s/号），不要调小。

### 3b. 增量更新（日常使用）

脚本对已有历史 CSV 的账号默认走**增量模式**：

- 从最新文章开始翻页；
- 某页所有链接都已在历史 CSV 中出现过，就**立即停止**，不再继续向后翻；
- 只把真正的新文章合并进该账号的 CSV/MD，历史数据不会被覆盖。

**抓取深度（重要）**：微信读书回填期（见“适用与不适用”）每页都混有回填的旧文，“整页已知即停”
长期不触发，会导致每次运行都翻穿全号（约 10 页/号 × 11 号 ≈ 110+ 次请求），这正是频繁触发
`-2041`/`-2014` 的直接原因。因此增量账号默认只翻**最近 14 天**（可用 `--crawl-days N` 或环境变量
`WR_CRAWL_DAYS` 调整）：翻到早于窗口起始日的文章即停，日请求量降到约 1～2 页/号。该窗口**只约束
抓取**，不影响汇总（汇总仍默认自 2026-07-10 起，二者已解耦）。

日常更新直接跑同一条命令即可：

```sh
node weread_extract.mjs 9223
```

**每周深扫一次**（建议固定某天、风控平静时跑）：补齐窗口之外的回填旧文，`--deep` 会翻穿整个号
（请求量与回填期旧行为相同，可能触发验证码，属预期——断点续跑会保存进度，下次继续）：

```sh
node weread_extract.mjs 9223 --deep
```

注意：显式 `--range YYYYMMDD-YYYYMMDD` 仍同时约束抓取与汇总窗口（旧行为不变）；`--deep`
不覆盖显式 `--range`；首次拉取（无 CSV）与 `--force` 全量模式不受滚动窗口限制。回填结束后
（预计数周），深扫也会自然在第 1～2 页停止，请求量自动回落。

为降低微信读书风控概率，脚本会复用本地缓存的 `bookId` 和当前书架，已在书架的公众号不再重复执行
“加入书架”；默认每页间隔 6 秒、每个公众号间隔 15 秒。不要在一次成功更新后立刻连续复跑全量账号。
如需进一步调慢，可设置 `WR_PAGE_INTERVAL_MS`、`WR_ACCOUNT_INTERVAL_MS` 和
`WR_READER_READY_WAIT_MS`，不建议调小默认值。

一旦接口返回 `-2041`，脚本会立即停止整个批次并跳过剩余账号，不再自动反复探测。请在专用 Chrome
中完成人工验证，等待 10～20 分钟，再用 `--check-session` 自检；通过后仅用 `--only 公众号名`
补跑失败账号。切换 VPN/代理、删除 Cookie、换新 Profile 或立刻反复重跑通常会让设备环境更不稳定。

断点续跑（深度制）：无论 `-2041` 还是 `-2014`，脚本在停止前都会把该号中断前已翻到的链接先
合并进它的 CSV/MD（即使该号整体标记为失败），并把**中断深度**（未验证到的 offset）记入
`进度.json` 的 `partialDepth`。之后该账号的运行会在中断深度之内**不触发**“整页已知即停”
（回填散布，中断点之上可能存在整页已知但缺口在更深处的页），翻过中断点后缺口自动补齐。
断点推进/清除规则：**限窗或 `--range` 运行不清除断点，只把断点前沿推进到**本次窗口边界与
原深度的较大者（窗口之下仍是未验证区，清除断点会让深扫在第一个整页已知处提前停、欠账永久
丢失）；只有当某次运行确实翻过断点深度（遇到真正的整页已知页，或翻到空页整号扫完）才清除。
日常 14 天窗口不足一页，通常在断点深度之上就停，断点会保留到下一次 `--deep` 深扫补齐为止。
注意：若账号仍处于 `-2014` 封控中，任何脚本都拿不到封控点之后的页，需先等待解封或换备用
窗口（9224）补跑；补齐会在其后第一次越过断点深度的成功运行中自动完成。运行报告与进度中以
`partialSaved` 标注中断前落盘的条数。

需要强制全量重拉某个账号时：`node weread_extract.mjs 9223 --force`（真正翻穿整个号到空页
为止，不因已知链接提前停；合并时按 URL 去重所以历史不丢。请求量大（与深扫相同），仅排障时用）。

只更新某一个账号：`node weread_extract.mjs 9223 --only 国科大就业`。

### 3c. 双 Profile / 双账号切换（应对 -2014）

`-2014` 通常表示**当前微信号已被标记/受限**（非验证码、非登录失效），单纯等待或重新扫码往往无效，需要**更换微信号**。为免每次手动切换登录，可用两个独立 Chrome Profile 分别登录两个微信读书账号，出现 `-2014` 时切到另一个窗口补跑：

- 主力窗口：`./start_weread_chrome.sh`（Profile `.browser-profile/weread`，CDP 端口 9223）
- 备用窗口：`./start_weread_chrome_backup.sh`（Profile `.browser-profile/weread-备用`，CDP 端口 9224）

两个窗口分别扫码登录**不同的**微信读书账号。抓取命令末尾的端口号对应要用的窗口：

```sh
node weread_extract.mjs 9223 --only 国科大就业   # 用主力账号补跑
node weread_extract.mjs 9224 --only 国科大就业   # 主力被 -2014 时改用备用账号补跑
```

注意：两账号书架上每个公众号都会被脚本重新订阅，不影响输出内容（文章列表是公开数据）；备用窗口同样走 `start_weread_chrome_backup.sh` 启动，登录状态写回各自 Profile。

### 3d. 时间范围与汇总格式

拉取范围可用 `--range YYYYMMDD-YYYYMMDD` 控制：晚于截止日的文章跳过，翻到早于起始日的文章即停：

```sh
node weread_extract.mjs 9223 --range 20260710-20260805
```

汇总（`汇总-全部公众号.csv` / `.md`）默认输出**自 2026-07-10 起的全部结果（不截断）**；指定 `--range` 后按该期限汇总。
汇总 Markdown 结构为：日期 = 二级标题，公众号名 = 三级标题；重复组的主链接正常显示，
其余成员链接收敛为“同题 N 篇”的次级行。

同时会生成**自包含 HTML**：`output/微信读书汇总-<起止日期>.html`（如
`output/微信读书汇总-20260710-20260805.html`），主链接突出，重复组成员折叠在
“同题 N 篇，点击展开”里弱化显示（灰色小字 + 日期/公众号标注）。
所有链接点击后在**新标签页**打开，已点过的链接会打上“✓已点”标记并变淡（用
localStorage 记住，重开文件仍有效），右下角有“清除已点标记”按钮，方便每天快速浏览不重复点。
已点击状态会按合并组传播：组内任一链接被点击后，整组（包括后续新增日期的同题链接）
都会标记为“✓已点”，并写入 `output/已点击链接.json`。

多人或多设备日常使用时，推荐启动 [`网页服务`](./web/README.md)。服务直接读取
`汇总-去重组.json`，新一次抓取完成后自动更新；每名用户的点击状态实时写入 SQLite，
不再需要手动导入/导出 HTML 点击记录。原自包含 HTML 继续保留，作为离线查看与兼容出口。

汇总去重分两层：

1. 完全相同的标题合并为一组（组内全部链接都保留）；
2. 相似度较高的组用本地 vLLM 部署的 `zai-org/GLM-5.3-Flash`（OpenAI 兼容 API，
   `DEDUP_API_BASE` / `DEDUP_MODEL` 可改）做语义去重确认。

**品牌主体硬否决（第三层，先于模型）**：同一集团下不同子公司是不同雇主、各自独立发招聘
（如阿里云、淘天、盒马互不相同，也都不是“阿里巴巴集团”那次招聘）。`generate_summary.py` 内置
品牌族谱表（别名 + 母子关系，最长匹配提取），把标题归约出“实际招聘主体”；主体不同的对
直接判为非重复——不送模型、也不复用历史结论（会修正以前的误合并，如曾经的 26 条阿里大组
被拆成 阿里云12 / 阿里巴巴8 / 淘天5 / 盒马1）。多品牌同现的标题（宣讲会/双选会）不否决，
仍交模型；网易、腾讯等“母子公司常指同一次校招”的族只对子品牌之间否决。运行时会打印
否决对数，若最终仍有组内主体混杂会输出 ⚠️ 审计告警。新增集团/品牌别名直接编辑
`generate_summary.py` 里的 `BRAND_FAMILY` 与 `STRICT_ROOTS`。

每次生成会复用上次 `汇总-去重组.json` 的分组结论：旧链接且标题没变的候选对直接沿用
上次“同组/不同组”的判断，不再重复调用 DeepSeek；只有新链接或标题变化的对才重新判断。
逐对判断结果也会写回 `汇总-去重组.json`，后续增量更新只处理新出现的候选对。
需要强制全量重算时加 `--no-reuse-groups`。语义去重的模型请求为**并发**执行（默认 4 路，
`--concurrency N` 或环境变量 `WR_DEDUP_CONCURRENCY` 可调；API 端点可承受一定并发，
批量重算时速度约提升为原来的 N 倍，单批失败仍整体报错不静默丢对）。

密钥读取顺序：项目根目录 `.env`（`DEEPSEEK_API_KEY=...`）→ 环境变量 `DEEPSEEK_API_KEY` → `--api-key`。
`.env` 已在 `.gitignore` 中，不会提交；密钥绝不写入输出文件。未提供密钥时脚本直接报错，
不会降级为仅同名去重。

单独重新生成汇总（不重新拉取），用本机 uv 执行：

```sh
DEEPSEEK_API_KEY=你的key uv run python generate_summary.py --range 20260710-20260805
```

`weread_extract.mjs` 在每次抓取结束后会自动用 `uv run python` 跑一次汇总（Docker 只承载
网页服务，不承载汇总），并把 uv 缓存固定到项目内 `.uv-cache/`，避免 `$HOME/.cache` 权限问题。
密钥（`DEDUP_API_BASE` / `DEDUP_MODEL` / `DEDUP_API_KEY`）从项目根目录
的 `.env` 读取；指定时间范围用 `--range`，强制全量重算语义去重加 `--no-reuse-groups`。

### 4. 产物

输出在 `output/weread_extract/`：

- `<公众号名>.csv` / `.md`：单号全量链接（发布日期、标题、链接）。
- `汇总-全部公众号.csv`：所有号合并、按发布日期倒序（默认自 2026-07-10 起全部结果，`--range` 可改窗口）。
- `汇总-全部公众号.md`：同窗口的去重版 Markdown（日期二级标题、公众号三级标题，同名/语义重复合并为组，重复链接收敛显示）。
- `output/微信读书汇总-<范围>.html`：排版优化版 HTML（重复组折叠弱化、新标签页打开、已点标记持久化）。
- `汇总-去重组.json`：去重组详情（canonical + 全部成员链接）。
- `运行报告.json`：每号 bookId、条数、时间范围、失败原因。
- `进度.json`：断点续跑状态。

## 对账与增量

- 与本地 `output/已发现链接.json` 对比时注意 URL 形式：本地缓存里既有短链 `/s/<id>`，也有长链 `?__biz=...&mid=...&sn=...`，同一文章两种形式都存在，直接比 URL 会漏判；需要按 `__biz+mid+idx+sn` 或标题规范化后对账。
- 增量：脚本以“整页均为已收录链接”作为停止条件，比按时间戳更稳（同一次群发的多篇文章时间戳相同）；微信读书接口本身有收录延迟（数小时～数天），当天晚些或次日再跑即可补上。

## 常见失败

| 现象 | 处理 |
| --- | --- |
| 接口返回 `-2010` | 微信读书登录失效，重新扫码 |
| 接口返回 `-2014` | 当前微信号被标记/受限，需更换微信号登录（见 3c 双 Profile 双账号切换）；也可等待后重试 |
| 接口返回 `-2041` | 可能是验证码拦截或不在阅读器页上下文。若脚本提示验证码，按 Chrome 窗口中提示完成，脚本会自动等待并重试；仍失败则重开 `/web/reader/<hash>` 再跑 |
| 页面出现验证码 | 停手，手动完成，隔几小时再跑；不要连续重试 |
| 书架里找不到该号 | 该号未被微信读书收录，换方案（微信客户端历史页） |
| 页面号名与目标不一致 | 种子链接给错号了，换一篇该号的链接 |

## 安全与合规

- 只读取公开文章的标题与链接，不抓正文、不改写任何内容。
- 控制在低频个人使用；不要商业化批量采集，2025 年后已有相关司法判例。
- 不要在本机缓存目录之外复制微信登录凭据；脚本不读取、不落盘 Cookie。
