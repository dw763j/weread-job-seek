# AGENTS.md

## 每日更新公众号链接的注意事项

- 每天在开始更新微信公众号链接前，先要求用户去专用 Chrome 窗口完成验证码（点击/拖动验证码），再由用户确认后运行提取脚本。
- 抓取过程中若接口返回 `-2041`（微信读书验证码拦截），脚本会停止整个批次。此时**不要担心风控**，停下后提醒用户去专用 Chrome 窗口输入验证码，用户确认后再重跑剩余账号（可用 `--only <号名>` 逐个补跑），而不是跳过剩余账号。
- `-2041` 是验证码拦截：**先要求用户在专用 Chrome 窗口完成验证码，不要直接启用备用账号**。仅当接口返回 `-2014`（当前微信号被标记/受限，非验证码）时，才切换到备用窗口（端口 9224）用 `--only` 补跑剩余账号。
- 日常更新直接跑 `node weread_extract.mjs 9223`：增量账号默认只翻最近 14 天，不要为日常更新加 `--deep` 或调大 `--crawl-days`（微信读书回填期翻穿全号是频繁触发风控的主因）。
- 每周做一次全量深扫补齐回填旧文：`node weread_extract.mjs 9223 --deep`（约 10 页/号，请求量大，属预期；若中途被 `-2041`/`-2014` 掐断，断点续跑已保存进度，按上面两条规则处理后重跑即可）。
- 更新全部跑完后（含补跑完剩余账号），先跑 AI 筛选补齐缺漏：`uv run python web/screen_update.py --all`（v2 结果直接复用，只筛没筛过/过时的组，包括回填到旧日期的链接；失败会记为「需人工复核」条目，下次运行自动重试，不阻塞后续提交）。2026-09-09 已完成 v1→v2 的全量补析，此后 --all 恢复只补缺的常态。
- 筛选跑完后，提交并推送本次数据变更：`git add output web/data/clicks.sqlite3 web/data/screen_results.json && git commit -m "data: 每日更新 <YYYY-MM-DD>（新增 <N> 条链接）" && git push`。N 取 `output/weread_extract/运行报告.json` 的 `new_links_this_run`；代码/文档改动另开提交，不混进数据提交。若 AI 筛选失败，链接数据照常提交推送，筛选留到下次补跑。
