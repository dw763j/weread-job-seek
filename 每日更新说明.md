# 每日更新说明

## 收录规则

- 只判断公众号名称是否出现在 `目标公众号.txt`。
- 目标公众号的消息全部收录，不做标题关键词筛选。
- `类型`只用于阅读时标注，不参与收录判断。
- 同一文章链接跨天、跨次运行自动去重。

## 输出

- `output/微信公众号招聘链接.csv`：目标公众号历史总表。
- `output/微信公众号招聘链接.md`：历史总表的 Markdown 版本。
- `output/每日更新/YYYY-MM-DD.csv`：当日首次发现的链接。
- `output/每日更新/YYYY-MM-DD.md`：当日链接的 Markdown 版本。
- `output/已发现链接.json`：去重状态，请勿随意删除。

## 完整更新方式

在 Codex 中说“更新公众号链接”或显式调用
`$update-wechat-public-account-links`。该 Skill 会依次检查
`目标公众号.txt` 中的公众号，逐篇打开更新消息，再提取、核对和生成日报。

完整流程由用户手动发起，不依赖特定操作系统或定时任务。微信需要保持登录；首次遇到登录或扫码界面时，由用户完成登录后继续。

只重新整理已加载缓存时，也可手动运行：

在项目根目录直接运行：

```bash
uv run python extract_wechat_articles.py
```

注意：该缓存扫描模式只能在 macOS 自动发现微信客户端缓存。Ubuntu 上请使用微信读书全量提取流程；若已有从其他机器复制过来的 `Share Data` SQLite 文件，可显式执行 `uv run python extract_wechat_articles.py --database /path/to/'Share Data'`。

## 更新后筛选与提交推送

每日更新全部跑完（含补跑）后，先跑 AI 筛选补齐缺漏（复用已有结果，只筛没筛过的组，含回填到旧日期的链接）：

```bash
uv run python web/screen_update.py --all
```

然后把数据变更提交并推送（运行日志已由 `.gitignore` 排除；筛选失败时链接数据照常提交，筛选下次补跑）：

```bash
git add output web/data/clicks.sqlite3 web/data/screen_results.json
git commit -m "data: 每日更新 $(date +%F)（新增 <N> 条链接）"
git push
```

新增条数以 `output/weread_extract/运行报告.json` 的 `new_links_this_run` 为准；代码与文档改动另开提交，不与数据提交混合。
